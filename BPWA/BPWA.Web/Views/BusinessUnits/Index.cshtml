@model BusinessUnitSearchModel

<div class="kt-portlet kt-portlet--mobile col-lg-12">
    <form method="POST" class="datatable_search_form">
        
        <input asp-for="IsDeleted" type="hidden" />
        
        <div class="kt-portlet__head kt-portlet__head--lg">
            <div class="kt-portlet__head-label">
                <span class="kt-portlet__head-icon">
                    <i class="kt-font-brand kt-menu__link-icon flaticon-grid-menu-v2"></i>
                </span>
                <h3 class="kt-portlet__head-title">
                    @Translations.BusinessUnits
                </h3>
            </div>
            <div class="kt-portlet__head-toolbar">
                <div class="kt-portlet__head-wrapper">
                    <div class="kt-portlet__head-actions">
                        <button data-url="@Url.Action("Add", "BusinessUnits")" class="open_modal_button btn btn-brand btn-elevate btn-icon-sm" type="button">
                            <i class="la la-plus"></i>
                            @Translations.New_record
                        </button>
                        <button class="btn btn-default datatable_search_form_reset_button">
                            <span>@Translations.Clear_filters<i class="flaticon-cancel ml-2"></i></span>
                        </button>
                        <button class="btn btn-default" type="submit">
                            <span>@Translations.Search<i class="la la-search ml-2"></i></span>
                        </button>
                    </div>
                </div>
            </div>
        </div>
        <div class="kt-portlet__head kt-portlet__head--lg">
            <div class="col-md-12 row mt-4">
                <div class="col-md form-group">
                    <input asp-for="Name" placeholder="@Translations.Name" class="form-control kt-input">
                </div>
                <div class="col-md-6 form-group">
                    <select asp-for="CompanyId" class="form-control select2"></select>
                </div>
            </div>
        </div>
    </form>

    <div class="kt-portlet__body">

        <table class="datatable"></table>

    </div>
</div>

<script>

    $(document).ready(function () {

       Select2AjaxCompanies({
            element: '#@nameof(BusinessUnitSearchModel.CompanyId)',
       });

       DatatableAjax({
            element: '.datatable',
            url: '@Url.Action("IndexJson", "BusinessUnits")',
            searchFormElement: '.datatable_search_form',
            columns: [
                {
                    field: '@nameof(BusinessUnitDTO.Name).ToCamelCase()',
                    title: '@Translations.Name',
                },
                {
                    template: (row) => DatatableBasicActions({
                        detailsUrl: `@Url.Action("Details", "BusinessUnits")/${row.id}`,
                        editUrl: `@Url.Action("Edit", "BusinessUnits")/${row.id}`,
                        deleteUrl: `@Url.Action("Delete", "BusinessUnits")/${row.id}`
                    }),
                    ...DatatableActionsColumnOptions(),
                }
            ]
        });
    });

</script>


